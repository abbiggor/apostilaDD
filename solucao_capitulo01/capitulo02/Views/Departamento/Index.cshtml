@model IEnumerable<capitulo02.Models.Departamento>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Criar novo departamento</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(model => item.Nome)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DepartamentoID">Editar departamento</a> |
                <a asp-action="Details" asp-route-id="@item.DepartamentoID">Exibir detalhes</a> |
                <a asp-action="Delete" asp-route-id="@item.DepartamentoID">Deletar</a>
            </td>
        </tr>
}
    </tbody>
</table>
